name: 'Build actions'
on: [push]
jobs:
  build:
    runs-on: macos-latest
    strategy:
      matrix:
        node-version: [12, 14]
    steps:
      - uses: actions/checkout@v2
      - name: 'Install node.js dependencies'
        run: npm install
      - name: 'Build'
        run: npm run build
      - name: 'Lint'
        run: npm run lint
  deploy:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@master
      - uses: appleboy/scp-action@master
      - name: 'Deploy indexes'
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT }}
          source: 'index.js,dist/index.html'
          target: ${{ secrets.SERVER_TARGET }}
      - name: 'Deploy favicon'
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT }}
          source: 'dist/favicon.ico'
          target: ${{ secrets.SERVER_PUBLIC_TARGET }}
      - name: 'Deploy css files'
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT }}
          source: 'dist/css/*'
          target: ${{ secrets.SERVER_PUBLIC_TARGET }}/css
      - name: 'Deploy fonts files'
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT }}
          source: 'dist/fonts/*'
          target: ${{ secrets.SERVER_PUBLIC_TARGET }}/fonts
      - name: 'Deploy js files'
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT }}
          source: 'dist/js/*'
          target: ${{ secrets.SERVER_PUBLIC_TARGET }}/js
